KubeScope was installed for the {{ .Values.app.environment }} environment.

{{- if .Values.ingress.enabled }}
Open http://{{ .Values.ingress.host }}/info
{{- else }}
Run:
  kubectl port-forward service/{{ include "kubescope.fullname" . }} 3000:{{ .Values.service.port }}
Then open http://127.0.0.1:3000/info
{{- end }}
