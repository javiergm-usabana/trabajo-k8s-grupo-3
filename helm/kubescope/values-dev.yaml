replicaCount: 1

image:
  repository: ghcr.io/javiergm-usabana/kubescope
  tag: "0f185ea"
  pullPolicy: IfNotPresent

imagePullSecrets:
  - name: ghcr-credentials

app:
  environment: dev
  version: "0f185ea"
  message: KubeScope - Development
  featureExperimental: true

ingress:
  enabled: true
  host: dev.kubescope.local

resources:
  requests:
    cpu: 25m
    memory: 48Mi
  limits:
    cpu: 150m
    memory: 128Mi

autoscaling:
  enabled: false
